{
	"servers": {
      "aws-mcp-server": {
        "command": "${workspaceFolder}/bin/aws-mcp-server",
        "args": [],
        "env": {
          "AWS_REGION": "ap-southeast-5"
        }
      }
	},
}